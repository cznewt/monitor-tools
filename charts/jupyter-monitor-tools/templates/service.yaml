apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyter-monitor-tools.fullname" . }}
  labels:
    {{- include "jupyter-monitor-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupyter-monitor-tools.selectorLabels" . | nindent 4 }}
